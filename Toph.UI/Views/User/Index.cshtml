@model Toph.UI.Models.UserIndexModel

@{
    ViewBag.Title = Model.Username;
}

<hgroup class="title">
    <h1>@ViewBag.Title</h1>
</hgroup>

@if (Model.ShowManageLink)
{
    <p>
        @Html.ActionLink("Manage", "Manage", "Account") your account
    </p>
}
